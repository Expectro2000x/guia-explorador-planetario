{
  "installCommand": "npm install --legacy-peer-deps",
  "version": 2,
  "builds": [
    {
      "src": "index.ts",
      "use": "@vercel/node",
      "entrypoint": "index.js"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ],
  "buildCommand": "npm run build"
}
